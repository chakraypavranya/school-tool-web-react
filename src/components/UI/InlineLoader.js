import React from 'react'
import { Loader } from 'semantic-ui-react'

const InlineLoader = () => 
(
    <Loader active inline='centered' className="pos-absolute" size='large'/>
  )

export default InlineLoader